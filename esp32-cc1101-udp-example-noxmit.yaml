packages:
  baseline: !include esp32-cc1101-udp-example.yaml

# example for testing receive standalone
remote_receiver:
  - id: !extend rf_receiver
    pin:
      allow_other_uses: false

remote_transmitter: !remove
button: !remove
